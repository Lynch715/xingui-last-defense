# 三段式血拼 Implementation Plan · Part 2（Task 4–8）

> 承接 `2026-07-29-三段式血拼.md`。同一份计划的续篇，执行方式相同。

---

## Task 4: finishBattle、三态结局与 resolveBattle 包装器

`resolveBattle` 降级为「三段全选 hold」的包装器：既有测试一行不用改，同时充当平衡回归夹具。

**Files:**
- Modify: `app.js`（替换原 `resolveBattle` 整个函数体）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
const fin=game.createInitialState("沈结算","yi","standard");
fin.crew=200;fin.morale=95;fin.territories.south_dock.guard=8;
const fr=game.resolveBattle(fin,{targetId:"south_dock",leaderIds:["player","zhaokui","chengye"],troops:150,tactic:"assault"},()=>.99);
assert.equal(fr.won,true);
assert.equal(fr.outcome,"win");
assert.equal(fin.territories.south_dock.owner,"player");
assert.equal(fin.battleSession,null,"结算后必须清空会话");
assert.equal(fin.winStreak,1,"胜场连胜计数");
assert.equal(fr.stages.length,3,"三段战报来自真实判定");

// 平衡回归：势必须真的响应兵力差。用已有的 seeded LCG 跑蒙特卡洛，
// 断言「投入更多兵 → 胜率显著更高」，并确认两端都不是必胜/必败的死区。
function winRate(troops,seed,n){
  let w=0;
  for(let k=0;k<n;k++){
    const t=game.createInitialState("沈平衡","yi","standard");
    t.crew=400;t.morale=62;t.territories.south_dock.guard=46;
    if(game.resolveBattle(t,{targetId:"south_dock",leaderIds:["player","zhaokui","chengye"],troops,tactic:"steady"},seeded(seed+k)).won)w++;
  }
  return w/n;
}
const weak=winRate(45,11000,150),strong=winRate(160,11000,150);
assert.ok(strong>weak+.3,`兵力差必须显著改变胜率：45人 ${weak} vs 160人 ${strong}`);
assert.ok(weak>0,"弱势方不应是必败死区（旧版 ratio<0.848 必败）");
assert.ok(strong<1,"优势方不应是必胜死区（旧版 ratio>1.191 必胜）");
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node tests/structure.test.mjs`
Expected: `AssertionError` 或 `finishBattle is not defined`

- [ ] **Step 3: 实现**

把 `app.js` 中原有的整个 `function resolveBattle(s,{targetId,leaderIds,troops,tactic},rng=Math.random){...}`（含其后续行，直到 `buildBattleNarrative` 之前）替换为：

```js
function finishBattle(s,rng=Math.random){
  const session=s.battleSession;if(!session)return null;
  const {targetId,tactic,troops}=session,t=s.territories[targetId],oldOwner=t.owner;
  const won=session.outcome==="win",retreated=session.outcome==="retreat";
  const leaders=session.leaderIds.map(id=>officer(s,id)).filter(Boolean);
  const meritMult=won&&session.leaderIds.includes("tangji")?1.5:1;                 // 唐霁「唯能者居」
  s.battles++;s.lastBattleMonth=s.month;s.training=Math.max(0,(s.training||0)-8);
  change(s,"heat",won?8:retreated?3:5);
  leaders.forEach(o=>{o.battles++;o.merit+=Math.round((won?5:2)*meritMult);o.exp+=won?3:1;o.loyalty=clamp(o.loyalty+(won?2:-2));if(o.id==="aqi"){const k=pick(["force","command","scheme","charm"],rng);o.stats[k]=clamp(o.stats[k]+rand(1,2,rng),1,99)}});
  const injured=[];leaders.forEach(o=>{if(o.id!=="player"&&!o.injured&&chance(won?.08:retreated?.05:.18,rng)){o.injured=rand(1,3,rng);injured.push(o.name)}});
  let captured=null;
  if(won){
    s.wins++;s.winStreak=(s.winStreak||0)+1;
    change(s,"morale",9);change(s,"rep",7);change(s,"support",t.stability>=55?2:-2);
    addCash(s,Math.round(TERRITORY_DEFS[targetId].income*.8));
    t.owner="player";t.guard=Math.max(16,Math.round((troops-session.losses)*.45));
    t.stability=s.creed==="yi"?62:s.creed==="wei"?42:52;s.intel[targetId]=true;
    if(session.mods.convertRate>0){const gain=Math.round(session.enemyLoss*session.mods.convertRate);if(gain>0){s.crew+=gain;log(s,"good",`程野把 ${gain} 名对方的人带回了老街。`)}}
    const lts=factionLeaders(s,oldOwner).filter(o=>!["hewanshan","fangjingyao","guchangfeng"].includes(o.id));
    if(lts.length&&territoryCount(s,oldOwner)===0)captured=lts[0];
    log(s,"good",`和联胜拿下了${TERRITORY_DEFS[targetId].name}，伤${session.losses}人。`);
  }else{
    s.winStreak=0;
    if(retreated){
      if(!session.mods.retreatShield)change(s,"morale",-6);                        // 叶蓉在阵则不掉士气
      change(s,"rep",-2);
      if(session.mods.pressed)resent(s,"zhaokui",8);                               // 听了赵魁又收手
      log(s,"warn",`队伍从${TERRITORY_DEFS[targetId].name}撤了回来，折损${session.losses}人。`);
    }else{
      s.losses++;change(s,"morale",-10);change(s,"rep",-3);
      t.guard=Math.max(12,t.guard-session.enemyLoss);
      leaders.forEach(o=>o.resentment=clamp(o.resentment+2));
      log(s,"bad",`进攻${TERRITORY_DEFS[targetId].name}失利，折损${session.losses}人。`);
    }
    if(session.leaderIds.includes("xiejiu"))loyalty(s,"xiejiu",-6);                // 谢九败北忠诚共 -8
  }
  const report={targetId,targetName:TERRITORY_DEFS[targetId].name,oldOwner,leaders:session.leaderIds.slice(),troops,tactic,won,outcome:session.outcome,losses:session.losses,enemyLoss:session.enemyLoss,injured,captured:captured?.id||null,ratio:session.ratio,momentum:session.momentum,stages:session.log.slice()};
  s.lastBattle=report;s.battleSession=null;
  if(won){markStyle(s,s.creed,1);checkFactionDefeat(s,oldOwner,captured);checkVictory(s)}
  saveGame();return report;
}

// 无头/自动战斗：三段全选 hold。既有测试沿用它，也是平衡回归夹具。
function resolveBattle(s,plan,rng=Math.random){
  startBattle(s,plan,rng);
  while(s.battleSession)applyStageChoice(s,"hold",rng);
  return s.lastBattle;
}
```

在 `module.exports` 追加 `finishBattle`。

**注意：** 原 `buildBattleNarrative` 已不再被调用（战报改由 `stageText` 逐段生成）。删除该函数及其调用。

- [ ] **Step 4: 运行确认通过**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node --check app.js && node tests/structure.test.mjs`
Expected: `structure and core-loop tests passed`

- [ ] **Step 5: 提交**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大/app.js 模拟社团老大/tests/structure.test.mjs
git commit -m "feat(battle): add finishBattle with win/loss/retreat outcomes

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5: 撤退代价

**Files:**
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// 复用的建档助手：把一名命名头目以我方身份塞进队伍
function joinNamed(state,id){
  const d=game.CHARACTER_DEFS[id];
  state.officers.push({id,name:d.name,side:"player",role:d.role,type:d.type,portrait:d.portrait,
    stats:{...d.stats},trait:d.trait,traitText:d.traitText,loyalty:70,resentment:0,merit:0,
    injured:0,exp:0,battles:0,wins:0,named:true});
  return state;
}

// quitAt=2 表示第2段鸣金；quitAt=0 表示打满三段
function runBattle(quitAt,extraLeader){
  const g=game.createInitialState("沈撤退","yi","standard");
  g.crew=200;g.morale=70;
  if(extraLeader)joinNamed(g,extraLeader);
  const ids=["player","zhaokui"];if(extraLeader)ids[1]=extraLeader;
  const rng=()=>.5;
  game.startBattle(g,{targetId:"south_dock",leaderIds:ids,troops:120,tactic:"steady"},rng);
  for(let stage=1;stage<=3&&g.battleSession;stage++){
    game.applyStageChoice(g,quitAt===stage?"withdraw":"hold",rng);
  }
  return g;
}
const quit=runBattle(2),full=runBattle(0);
assert.equal(quit.lastBattle.outcome,"retreat");
assert.ok(full.lastBattle,"打满三段必须结算出战报");
assert.ok(quit.lastBattle.losses<full.lastBattle.losses,"第2段鸣金的伤亡必须严格小于打满三段");
assert.notEqual(quit.territories.south_dock.owner,"player","撤退不夺地");
assert.equal(quit.morale,64,"撤退士气 70-6=64");

const shielded=runBattle(2,"yerong");
assert.equal(shielded.lastBattle.outcome,"retreat");
assert.equal(shielded.morale,70,"叶蓉在阵撤退不掉士气");
```

- [ ] **Step 2: 运行确认失败或通过**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node tests/structure.test.mjs`
Expected: PASS（Task 4 已实现撤退逻辑，本任务是为其补测试）。若 FAIL，按报错修 `finishBattle` 的撤退分支。

- [ ] **Step 3: 提交**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大/tests/structure.test.mjs
git commit -m "test(battle): cover retreat cost and 叶蓉 morale shield

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6: 头目专属提议

填充 Task 2 留下的 `officerProposals` 空壳。

**Files:**
- Modify: `app.js`（替换 `officerProposals`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
const prop=game.createInitialState("沈提议","yi","standard");
prop.crew=200;
game.startBattle(prop,{targetId:"south_dock",leaderIds:["player","sumanqing","chengye"],troops:100,tactic:"steady"});
const ps=prop.battleSession;
assert.ok(game.stageOptions(prop,ps).some(o=>o.id==="flank"),"苏曼青谋略88应给出侧翼提议");
assert.ok(!game.stageOptions(prop,ps).some(o=>o.id==="parley"),"势≤20 时不给劝降");
ps.momentum=25;
assert.ok(game.stageOptions(prop,ps).some(o=>o.id==="parley"),"势>20 才解锁劝降");
assert.ok(game.stageOptions(prop,ps).length<=5,"选项上限5");
assert.ok(game.stageOptions(prop,ps).some(o=>o.id==="hold"),"稳住必须恒在");
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node tests/structure.test.mjs`
Expected: `AssertionError: 苏曼青谋略88应给出侧翼提议`

- [ ] **Step 3: 实现**

把 `function officerProposals(s,session,inLineup){return[]}` 整行替换为：

```js
// 效果按属性缩放，不是固定值——否则杂鱼说客和程野没区别。
function officerProposals(s,session,inLineup){
  const out=[],sm=inLineup("sumanqing")?officer(s,"sumanqing"):null,cy=inLineup("chengye")?officer(s,"chengye"):null;
  if(sm&&session.stage<=2&&sm.stats.scheme>=70)
    out.push({id:"flank",speaker:"苏曼青",text:"「他们左翼是空的」",effect:"势↑ 伤亡↓",mult:1+sm.stats.scheme/900,casualty:.9});
  if(inLineup("weixiaolou")&&!s.intel[session.targetId])
    out.push({id:"backdoor",speaker:"魏小楼",text:"「后门我一直留着」",effect:"势↑ 当场揭穿驻防",mult:1.12,casualty:1});
  if(cy&&session.momentum>20)
    out.push({id:"parley",speaker:"程野",text:"「让我去喊一嗓子」",effect:"胜则收编对方的人",mult:.95,casualty:1,convert:cy.stats.charm/200});
  if(inLineup("yerong")&&session.stage<=2)
    out.push({id:"supply",speaker:"叶蓉",text:"「退路和粮草我安排好了」",effect:"伤亡↓↓",mult:1,casualty:.75});
  if(duelChallenger(s,session)&&duelTarget(s,session))
    out.push({id:"duel",speaker:duelChallenger(s,session).name,text:"「那个人交给我」",effect:"单挑：胜则压制，败则受伤",mult:1,casualty:1});
  if(inLineup("aqi")&&session.stage>=2)
    out.push({id:"rearguard",speaker:"阿七",text:"「我来断后」",effect:"伤亡↓ 阿七成长更快",mult:1,casualty:.85});
  return out;
}
// 设计稿表格写「敌方有猛将」，5.4 节写「武力最高者」——以 5.4 为准并加 force>=60 下限，
// 否则全局只有韩彪一人算猛将，单挑几乎不会出现。
function duelTarget(s,session){return factionLeaders(s,s.territories[session.targetId].owner).filter(o=>o.stats.force>=60).sort((a,b)=>b.stats.force-a.stats.force)[0]||null}
function duelChallenger(s,session){return["hanbiao","zhaokui","xiejiu"].map(id=>session.leaderIds.includes(id)?officer(s,id):null).filter(o=>o&&!o.injured).sort((a,b)=>b.stats.force-a.stats.force)[0]||null}
```

在 `applyStageChoice` 中，`if(opt.id==="press")session.mods.pressed=true;` 之后追加：

```js
  if(opt.id==="backdoor"){s.intel[session.targetId]=true;extra=`魏小楼把${TERRITORY_DEFS[session.targetId].name}的真实驻防摊在了你面前。`}
  if(opt.id==="parley")session.mods.convertRate=opt.convert;
  if(opt.id==="rearguard"){const a=officer(s,"aqi");if(a)a.exp+=3}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node --check app.js && node tests/structure.test.mjs`
Expected: `structure and core-loop tests passed`

- [ ] **Step 5: 提交**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大/app.js 模拟社团老大/tests/structure.test.mjs
git commit -m "feat(battle): officer-proposed stage options scaled by stats

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7: 单挑

**Files:**
- Modify: `app.js`
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
// joinNamed 在 Task 5 已定义，此处复用
const duel=joinNamed(game.createInitialState("沈单挑","wei","standard"),"hanbiao");
duel.crew=200;
game.startBattle(duel,{targetId:"south_dock",leaderIds:["player","hanbiao"],troops:100,tactic:"assault"});
assert.ok(game.stageOptions(duel,duel.battleSession).some(o=>o.id==="duel"),"敌方有高武力头目时应给出单挑");
game.applyStageChoice(duel,"duel",()=>.01);   // rng 极小 → 单挑必胜
assert.ok(duel.battleSession.mods.multRest>1,"单挑胜利提高后续段的势");
assert.ok(duel.battleSession.log[0].text.includes("韩彪"),"单挑要写进战报");
assert.ok(duel.officers.some(o=>o.side==="east"&&o.injured>0),"败方头目应受伤");

// 韩彪不在阵时同样可单挑（赵魁顶上），但没有 +0.15
const noHan=game.createInitialState("沈无韩","wei","standard");
noHan.crew=200;
game.startBattle(noHan,{targetId:"south_dock",leaderIds:["player","zhaokui"],troops:100,tactic:"assault"});
assert.ok(game.stageOptions(noHan,noHan.battleSession).some(o=>o.id==="duel"),"赵魁也能发起单挑");
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node tests/structure.test.mjs`
Expected: `AssertionError: 单挑胜利提高后续段的势`

- [ ] **Step 3: 实现**

在 `duelChallenger` 之后插入：

```js
function resolveDuel(s,session,rng){
  const me=duelChallenger(s,session),foe=duelTarget(s,session);
  if(!me||!foe)return{won:false,text:""};
  let p=.5+(me.stats.force-foe.stats.force)/200;
  if(session.leaderIds.includes("hanbiao")&&!officer(s,"hanbiao")?.injured)p+=.15;  // 韩彪「顶门硬骨」
  p=clamp(p,.15,.85);                                                              // 加成后再夹取，上限不被突破
  if(chance(p,rng)){foe.injured=rand(1,3,rng);session.mods.multRest*=1.25;me.merit+=8;return{won:true,text:`${me.name}把${foe.name}逼到了墙角。`}}
  me.injured=rand(1,3,rng);session.mods.multRest*=.85;change(s,"morale",-5);
  return{won:false,text:`${me.name}没能压住${foe.name}，被抬了下去。`};
}
```

在 `applyStageChoice` 的 `if(opt.id==="rearguard")...` 之后追加：

```js
  if(opt.id==="duel")extra=resolveDuel(s,session,rng).text;
```

- [ ] **Step 4: 运行确认通过**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node --check app.js && node tests/structure.test.mjs`
Expected: `structure and core-loop tests passed`

- [ ] **Step 5: 提交**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大/app.js 模拟社团老大/tests/structure.test.mjs
git commit -m "feat(battle): add officer duel with 韩彪 suppression bonus

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8: 被动特性回归测试（沈川／谢九／唐霁）

三条被动已分别在 Task 1 与 Task 4 写入，本任务只补测试锁死它们。

**Files:**
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写测试**

```js
// 沈川：士气下限 45（低士气时伤亡不再随士气无限恶化）
const floorLow=game.createInitialState("沈低迷","yi","standard");
floorLow.crew=200;floorLow.morale=10;
game.startBattle(floorLow,{targetId:"south_dock",leaderIds:["player"],troops:120,tactic:"steady"});
assert.equal(floorLow.battleSession.mods.moraleFloor,45);
const withPlayer=(()=>{let i=0;const r=()=>[.5][i++%1];game.applyStageChoice(floorLow,"hold",r);return floorLow.battleSession.losses})();
const noFloor=game.createInitialState("沈无印","yi","standard");
noFloor.crew=200;noFloor.morale=10;
game.startBattle(noFloor,{targetId:"south_dock",leaderIds:["zhaokui"],troops:120,tactic:"steady"});
let m=0;const r3=()=>[.5][m++%1];
game.applyStageChoice(noFloor,"hold",r3);
assert.ok(withPlayer<noFloor.battleSession.losses,"沈川在阵应因士气下限而少死人");

// 唐霁：胜利时功劳 ×1.5
const merit=joinNamed(game.createInitialState("沈唯能","yi","standard"),"tangji");
merit.crew=250;merit.morale=95;merit.territories.south_dock.guard=6;
game.resolveBattle(merit,{targetId:"south_dock",leaderIds:["player","tangji"],troops:200,tactic:"assault"},()=>.99);
assert.equal(merit.lastBattle.won,true);
assert.equal(merit.officers.find(o=>o.id==="player").merit,8,"唐霁在阵：胜利功劳 5×1.5 取整为 8");

// 谢九：败北忠诚共 -8（joinNamed 给的初始忠诚是 70）
const xie=joinNamed(game.createInitialState("沈只服","yi","standard"),"xiejiu");
xie.crew=60;xie.morale=20;xie.territories.south_dock.guard=78;
game.resolveBattle(xie,{targetId:"south_dock",leaderIds:["xiejiu"],troops:20,tactic:"steady"},()=>.01);
assert.equal(xie.lastBattle.won,false);
assert.equal(xie.officers.find(o=>o.id==="xiejiu").loyalty,62,"70 -2(通用) -6(谢九) = 62");
assert.equal(xie.winStreak,0);
```

- [ ] **Step 2: 运行**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node tests/structure.test.mjs`
Expected: `structure and core-loop tests passed`。若某条 FAIL，回到 Task 1／Task 4 对应分支修正。

- [ ] **Step 3: 提交**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大/tests/structure.test.mjs
git commit -m "test(battle): lock in 沈川/唐霁/谢九 passive traits

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

Task 9–12 见 `2026-07-29-三段式血拼-part3.md`。
