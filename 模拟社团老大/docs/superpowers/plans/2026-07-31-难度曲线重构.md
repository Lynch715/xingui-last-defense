# 难度曲线重构 Part 1：兵力状态机与节奏闸门

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `s.crew` 从"一个永远够用的数字"改成"能战/整补/养伤"三池状态机，并让血拼消耗行动点，使 60 个月的战役框架第一次真正产生约束。

**Architecture:** 出战时人手立即离开 `s.crew`；战后幸存者进 `s.regroup`（约 2 个月归队）、伤亡的 55% 进 `s.wounded`（约 4～5 个月且要付医药费）、其余阵亡永久消失。`crewCap` 把人手上限绑定到地盘数，使"扩张"成为养更多人的唯一途径。血拼额外消耗 1 行动点。

**Tech Stack:** 原生 JavaScript（单文件 `app.js`，CommonJS 导出供 Node 测试）、`node:assert/strict`、无构建步骤。

**Spec:** `docs/superpowers/specs/2026-07-31-难度曲线重构-design.md`（第 3、7.1、9、10 节）

**后续:** Part 2 做敌方数值重配与主动扩张；Part 3 做驻防期、新行动、界面与数值校准。

---

## 文件结构

| 文件 | 职责 | 本 Part 的改动 |
|---|---|---|
| `app.js` | 全部游戏逻辑（单文件，函数以紧凑单行风格书写） | 新增 `totalCrew`/`crewCap`/`recoverCrew`；改 `createInitialState`、`startBattle`、`applyStageChoice`、`finishBattle`、`advanceMonth`、`monthlyUpkeep`、`recruit_crew`、`normalizeState`、导出表 |
| `tests/structure.test.mjs` | 既有结构与战斗回归测试（590 行，顺序执行的断言脚本） | 更新 4 条断言旧行为的断言；新增兵力池测试 |
| `tests/balance.test.mjs` | **新建**。用固定种子的策略机器人跑完整局，断言难度曲线 | 全新 |
| `tests/rush.sim.mjs` | 诊断用的一次性脚本 | 被 `balance.test.mjs` 取代后删除 |
| `README.md` | 检查流程说明 | 加一行 `node tests/balance.test.mjs` |

**风格约定：** `app.js` 现有代码是紧凑单行风格（`function f(s){...}` 全写在一行），注释用于解释"为什么"而非"是什么"。新增代码必须沿用这个风格，不要引入多行格式化的函数体，除非函数超过约 6 个语句。

---

### Task 1: 兵力三池的字段与派生函数

**Files:**
- Modify: `app.js:133`（`createInitialState` 的状态字面量）
- Modify: `app.js:137`（`officerCapacity` 之后插入新函数）
- Modify: `app.js:520`（导出表）
- Test: `tests/structure.test.mjs:13`（现有开局断言之后）

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 第 13 行 `assert.equal(s.crew,42);` 之后插入：

```javascript
assert.equal(s.regroup,0,"开局没有整补中的人");
assert.equal(s.wounded,0,"开局没有伤员");
assert.equal(game.totalCrew(s),42,"总人手=能战+整补+养伤");
assert.equal(game.crewCap(s),60,"开局只有老街一块地：40+1*20+0");
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，报 `game.totalCrew is not a function`

- [ ] **Step 3: 加字段**

`app.js:133`，在状态字面量里把 `crew:42,` 改成：

```javascript
crew:42,regroup:0,wounded:0,
```

- [ ] **Step 4: 加派生函数**

`app.js:137`（`function officerCapacity(s){...}` 那一行之后）插入：

```javascript
function totalCrew(s){return s.crew+(s.regroup||0)+(s.wounded||0)}
// 人手上限绑定地盘：想养更多人只能先拿地，拿地又需要人。这是本作扩张压力的主旋钮。
function crewCap(s){const ids=ownTerritories(s);return 40+ids.length*20+ids.reduce((sum,id)=>sum+((s.territories[id].level||1)-1),0)*6}
```

- [ ] **Step 5: 加导出**

`app.js:520`，在 `module.exports={` 的列表里 `monthlyGross,` 之前插入 `totalCrew,crewCap,`。

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: 无输出、退出码 0（该测试脚本静默通过）

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "feat(crew): 拆出整补/养伤两个池子与人手上限"
```

---

### Task 2: 出战即扣人，段内只记账

**Files:**
- Modify: `app.js:203-210`（`startBattle`）
- Modify: `app.js:294`（`applyStageChoice` 中扣人那一行）
- Test: `tests/structure.test.mjs:77`、`:142`、`:154`

- [ ] **Step 1: 改写既有的三条断言（它们锁定的正是要废除的旧行为）**

`tests/structure.test.mjs:77`，把

```javascript
assert.equal(bs.crew,120,"startBattle 本身不扣人");
```

改成

```javascript
assert.equal(bs.crew,60,"出战即扣人：120-60");
```

`tests/structure.test.mjs:142`，把

```javascript
assert.equal(crewBefore-adv.crew,adv.battleSession.losses,"扣的人必须与记录的伤亡完全相等");
```

改成

```javascript
assert.equal(adv.crew,crewBefore,"段内不再动人手池：人在开战时就离开了，伤亡只记在 session 上");
```

`tests/structure.test.mjs:154`，把

```javascript
assert.equal(fresh.crew,120,"被拒的撤退不得扣人");
```

改成

```javascript
assert.equal(fresh.crew,60,"被拒的撤退不得再额外扣人");
```

- [ ] **Step 2: 加一条新断言：伤亡不得超过出战人数**

在 `tests/structure.test.mjs:160`（`assert.equal(adv.battleSession.stage,stageOneStage,"抛错不得推进段数");`）之后插入：

```javascript
// 伤亡累计封顶在出战人数：否则 finishBattle 算幸存者会得到负数，人手池会凭空膨胀。
// 自然战斗打不满这个上限（每段约扣 4% 出战人数），所以直接把 losses 顶到临界值来验。
const capLoss=game.createInitialState("沈封顶","wei","standard");
capLoss.crew=200;
game.startBattle(capLoss,{targetId:"south_dock",leaderIds:["player"],troops:60,tactic:"steady"});
capLoss.battleSession.losses=59;
game.applyStageChoice(capLoss,"hold",()=>.5);
assert.ok(capLoss.battleSession.losses<=60,`伤亡 ${capLoss.battleSession.losses} 不得超过出战的 60 人`);
```

- [ ] **Step 3: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，第一条报 `Expected values to be strictly equal: 120 !== 60`

- [ ] **Step 4: 在 startBattle 里扣人**

`app.js:210`，把

```javascript
  s.battleSession={targetId,leaderIds:ids,troops,tactic,stage:1,momentum:0,ratio:est.ratio,losses:0,enemyLoss:0,outcome:"",mods,log:[]};
```

改成

```javascript
  s.crew-=troops;                                                                 // 人立刻离开能战池，直到 finishBattle 才分流回整补/养伤
  s.battleSession={targetId,leaderIds:ids,troops,tactic,stage:1,momentum:0,ratio:est.ratio,losses:0,enemyLoss:0,outcome:"",mods,log:[]};
```

- [ ] **Step 5: 段内不再扣人，并给伤亡封顶**

`app.js:294`，把

```javascript
  const loss=stageLoss(s,session,ahead,(opt.casualtyMult??1),rng);         // 伤亡按累计局势定档，避免优势方被单段波动多收血
```

改成

```javascript
  const loss=Math.min(stageLoss(s,session,ahead,(opt.casualtyMult??1),rng),session.troops-session.losses);  // 伤亡按累计局势定档；封顶在出战人数，否则幸存者会算成负数
```

同一函数下面一行，把

```javascript
  session.losses+=loss;s.crew=Math.max(1,s.crew-loss);s.casualties+=loss;
```

改成

```javascript
  session.losses+=loss;s.casualties+=loss;                                 // 人已不在池子里，这里只记账
```

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: 无输出、退出码 0

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "feat(battle): 出战即扣人，段内只记伤亡"
```

---

### Task 3: 战后把人分流回三个池子

**Files:**
- Modify: `app.js:309`（`finishBattle` 中 `won`/`retreated` 判定之后）
- Test: `tests/structure.test.mjs`（文件末尾追加）

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 末尾追加：

```javascript
// ---- 战后人手分流 ----
// 出战的人在 startBattle 离池，finishBattle 必须把他们分成幸存(整补)/重伤(养伤)/阵亡(消失)三份。
const settle=game.createInitialState("沈结算","yi","standard");
settle.crew=120;
game.resolveBattle(settle,{targetId:"south_dock",leaderIds:["player"],troops:60,tactic:"steady"},seeded(7));
const settleRep=settle.lastBattle;
const settleWounded=Math.round(settleRep.losses*.55);
assert.ok(settleRep.losses>0,"这场仗必须真的死人，否则断言无意义");
assert.equal(settle.crew,60,"出战的60人不得直接回到能战池");
assert.equal(settle.regroup,60-settleRep.losses,"幸存者全部进整补");
assert.equal(settle.wounded,settleWounded,"伤亡的55%进养伤");
assert.equal(game.totalCrew(settle),120-(settleRep.losses-settleWounded),"总人手只少了阵亡的那部分");

// 撤退与战败走同一条分流路径，不能只在胜利分支里结算。
const settleRetreat=game.createInitialState("沈撤退结算","yi","standard");
settleRetreat.crew=120;
game.startBattle(settleRetreat,{targetId:"south_dock",leaderIds:["player","sumanqing"],troops:60,tactic:"steady"});
game.applyStageChoice(settleRetreat,"hold",seeded(11));
game.applyStageChoice(settleRetreat,"withdraw",seeded(11));
assert.equal(settleRetreat.battleSession,null,"撤退后会话必须结束");
assert.equal(settleRetreat.regroup+settleRetreat.wounded>0,true,"撤退回来的人也要进整补/养伤，不能凭空消失");
assert.equal(settleRetreat.crew,60,"撤退不把人直接还回能战池");
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，报 `settle.regroup` 为 `undefined` 或 `0`，与期望不符

- [ ] **Step 3: 在 finishBattle 里分流**

`app.js:309`，在

```javascript
  const won=session.outcome==="win",retreated=session.outcome==="retreat";
```

之后插入：

```javascript
  // 出战的人在 startBattle 就离开了能战池，这里分三份收尾。阵亡的那部分永久消失——这是"打不起"的根源。
  const survivors=Math.max(0,session.troops-session.losses),woundedBack=Math.round(session.losses*.55);
  s.regroup=(s.regroup||0)+survivors;s.wounded=(s.wounded||0)+woundedBack;
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: 无输出、退出码 0

- [ ] **Step 5: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "feat(battle): 战后把出战人手分流进整补与养伤"
```

---

### Task 4: 每月回流 `recoverCrew`

**Files:**
- Modify: `app.js:167`（`monthlyNet` 之后插入新函数）
- Modify: `app.js:385`（`advanceMonth` 的月初处理）
- Modify: `app.js:520`（导出表）
- Test: `tests/structure.test.mjs`（文件末尾追加）

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 末尾追加：

```javascript
// ---- 每月回流 ----
const rec=game.createInitialState("沈回流","yi","standard");
rec.crew=0;rec.regroup=48;rec.wounded=10;rec.cash=100;
const recOut=game.recoverCrew(rec);
assert.equal(recOut.back,24,"整补每月回一半：ceil(48*0.5)");
assert.equal(rec.regroup,24);
assert.equal(recOut.healed,3,"养伤每月回 ceil(10*0.22)=3");
assert.equal(recOut.cost,4,"医药费 = 伤员数 * 0.4");
assert.equal(rec.cash,96,"医药费从现金里扣");
assert.equal(rec.wounded,7);
assert.equal(rec.crew,27,"24 整补归队 + 3 伤愈");

// 付不出药钱：回归减半、掉士气、且不扣钱（钱本来就不够）
const broke=game.createInitialState("沈没钱养伤","yi","standard");
broke.crew=0;broke.regroup=0;broke.wounded=10;broke.cash=1;
const brokeMorale=broke.morale;
const brokeOut=game.recoverCrew(broke);
assert.equal(brokeOut.broke,true);
assert.equal(brokeOut.healed,1,"付不起时回归减半：floor(3/2)");
assert.equal(brokeOut.cost,0,"付不起就不扣钱");
assert.equal(broke.cash,1);
assert.equal(broke.morale,brokeMorale-4);

// 尾数：整补只剩 3 人时，不能因为"至少回 5 人"而回出负数
const tail=game.createInitialState("沈收尾","yi","standard");
tail.crew=0;tail.regroup=3;tail.wounded=0;
assert.equal(game.recoverCrew(tail).back,3,"整补余数不足5人时一次归队完毕");
assert.equal(tail.regroup,0);
assert.equal(tail.crew,3);

// 空池不得产生任何副作用
const idle=game.createInitialState("沈无伤","yi","standard");
const idleCash=idle.cash,idleCrew=idle.crew;
const idleOut=game.recoverCrew(idle);
assert.deepEqual([idleOut.back,idleOut.healed,idleOut.cost],[0,0,0]);
assert.equal(idle.cash,idleCash);
assert.equal(idle.crew,idleCrew);

// 推进月份必须触发回流
const flow=game.createInitialState("沈过月","yi","standard");
flow.regroup=20;flow.ap=0;
game.advanceMonth(flow,true);
assert.ok(flow.regroup<20,"advanceMonth 必须调用 recoverCrew");
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，报 `game.recoverCrew is not a function`

- [ ] **Step 3: 实现 `recoverCrew`**

`app.js:167`（`function monthlyNet(s){...}` 那一行之后）插入：

```javascript
// 整补两个月归队、养伤四五个月且要花钱——"打完一仗伤不起"的实现在这里。
// 必须在 applyEconomy 之前调用：医药费要计入当月账面，否则资金链危机会晚一个月才发作。
function recoverCrew(s){
  const out={back:0,healed:0,cost:0,broke:false};
  if(s.regroup>0){out.back=Math.min(s.regroup,Math.max(5,Math.ceil(s.regroup*.5)));s.regroup-=out.back;s.crew+=out.back}
  if(s.wounded>0){
    const base=Math.min(s.wounded,Math.max(1,Math.ceil(s.wounded*.22))),cost=Math.round(s.wounded*.4*10)/10;
    if(s.cash>=cost){addCash(s,-cost);out.cost=cost;out.healed=base}
    else{out.broke=true;out.healed=Math.floor(base/2);change(s,"morale",-4);log(s,"bad","付不出伤者的药钱，养伤的人回得更慢了。")}
    s.wounded-=out.healed;s.crew+=out.healed;
  }
  if(out.back||out.healed)log(s,"good",`${out.back} 人整补归队，${out.healed} 人伤愈。`);
  return out;
}
```

- [ ] **Step 4: 接进 `advanceMonth`**

`app.js:385`，把

```javascript
  s.month++;s.ap=3;s.usedActions={};s.lastAction=null;applyEconomy(s);checkInsolvency(s);
```

改成

```javascript
  s.month++;s.ap=3;s.usedActions={};s.lastAction=null;recoverCrew(s);applyEconomy(s);checkInsolvency(s);
```

- [ ] **Step 5: 加导出**

`app.js:520`，在 `module.exports={` 列表里已加的 `totalCrew,crewCap,` 后面追加 `recoverCrew,`。

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: 无输出、退出码 0

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "feat(crew): 每月整补归队与伤员回流，养伤要付药钱"
```

---

### Task 5: 招人受上限约束，维护费按总人手算

**Files:**
- Modify: `app.js:63`（`ACTIONS` 里的 `recruit_crew`）
- Modify: `app.js:166`（`monthlyUpkeep`）
- Modify: `app.js:373`（`applyAction` 的 `toast` 调用）
- Modify: `app.js:448`（`renderHall` 的 `lockedText` 取值）
- Test: `tests/structure.test.mjs`（文件末尾追加）

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 末尾追加：

```javascript
// ---- 人手上限与维护费 ----
const capped=game.createInitialState("沈满员","yi","standard");
capped.cash=100;capped.crew=60;
assert.equal(game.crewCap(capped),60);
assert.equal(game.applyAction(capped,"recruit_crew"),false,"到上限就招不动了");
assert.equal(capped.crew,60,"被拒的招募不得改变人手");
assert.equal(capped.ap,3,"被拒的行动不得扣行动点");

const nearCap=game.createInitialState("沈快满","yi","standard");
nearCap.cash=100;nearCap.crew=55;
game.applyAction(nearCap,"recruit_crew");
assert.equal(game.totalCrew(nearCap),60,"招人不得越过上限");

// 整补和养伤的人也占上限：否则打完仗立刻能招满，池子形同虚设
const capCounts=game.createInitialState("沈占额","yi","standard");
capCounts.cash=100;capCounts.crew=10;capCounts.regroup=30;capCounts.wounded=20;
assert.equal(game.applyAction(capCounts,"recruit_crew"),false,"整补/养伤的人同样占用人手上限");

// 养伤的人也要吃饭
const up=game.createInitialState("沈养伤开销","yi","standard");
const upBase=game.monthlyUpkeep(up);
up.crew=22;up.regroup=10;up.wounded=10;
assert.equal(game.monthlyUpkeep(up),upBase,"维护费按总人手算，养伤的人不免费");
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，报 `到上限就招不动了` 处 `true !== false`

- [ ] **Step 3: 改 `recruit_crew`**

`app.js:63`，把整条 `recruit_crew` 定义替换为：

```javascript
  {id:"recruit_crew",icon:"众",name:"去老街招人",desc:"让程野在球场、码头和老街间找肯跟你的人。",effects:["人手↑↑","现金-5万"],max:2,canRun:s=>s.cash>=recruitCost(s,5)&&totalCrew(s)<crewCap(s),lockedText:s=>totalCrew(s)>=crewCap(s)?"老街养不下更多人了":"现金不足",run:s=>{const bonus=owns(s,"old_street")?2:0,mult=hasOfficer(s,"chengye")?1.25:1,gain=Math.min(Math.round((rand(7,12)+bonus)*mult),Math.max(0,crewCap(s)-totalCrew(s)));s.crew+=gain;addCash(s,-recruitCost(s,5));change(s,"morale",2);log(s,"good",`程野带回了 ${gain} 名新人。`)}},
```

- [ ] **Step 4: 让 `lockedText` 同时接受字符串和函数**

`app.js:166` 上方（`function monthlyUpkeep` 之前）插入：

```javascript
function lockedTextOf(a,s){return typeof a.lockedText==="function"?a.lockedText(s):a.lockedText}
```

`app.js:373` 的 `applyAction`，把

```javascript
if(a.canRun&&!a.canRun(s)){toast(a.lockedText||"当前条件不足");return false}
```

改成

```javascript
if(a.canRun&&!a.canRun(s)){toast(lockedTextOf(a,s)||"当前条件不足");return false}
```

`app.js:448` 的 `renderHall`，把模板里的

```javascript
unavailable?a.lockedText||"条件不足":"安排 · 1点"
```

改成

```javascript
unavailable?lockedTextOf(a,S)||"条件不足":"安排 · 1点"
```

- [ ] **Step 5: 改 `monthlyUpkeep`**

`app.js:166`，把

```javascript
function monthlyUpkeep(s){let crew=s.crew*.13;
```

改成

```javascript
function monthlyUpkeep(s){let crew=totalCrew(s)*.13;
```

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: 无输出、退出码 0

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "feat(crew): 招募受人手上限约束，维护费按总人手计算"
```

---

### Task 6: 血拼消耗 1 行动点

**Files:**
- Modify: `app.js:200-210`（`startBattle`）
- Modify: `app.js:448`（`renderHall` 的说明文字）
- Modify: `app.js:479`（`renderBattle` 的 `section-head` 说明文字）
- Test: `tests/structure.test.mjs`（文件末尾追加）

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 末尾追加：

```javascript
// ---- 血拼消耗行动点 ----
// 这是"5分钟通关"的根因之一：过去发起进攻零成本，一个月可以打无限场。
const apCost=game.createInitialState("沈行动点","yi","standard");
apCost.crew=200;
game.startBattle(apCost,{targetId:"south_dock",leaderIds:["player"],troops:60,tactic:"steady"});
assert.equal(apCost.ap,2,"开战消耗1个行动点");
while(apCost.battleSession)game.applyStageChoice(apCost,"hold",seeded(3));

const apBroke=game.createInitialState("沈没点数","yi","standard");
apBroke.crew=200;apBroke.ap=0;
assert.throws(()=>game.startBattle(apBroke,{targetId:"south_dock",leaderIds:["player"],troops:60,tactic:"steady"}),/no action point/);
assert.equal(apBroke.crew,200,"被拒的开战不得扣人手");

// 错误优先级：人手不足要先于行动点不足报出来，界面提示才对得上
const apOrder=game.createInitialState("沈两缺","yi","standard");
apOrder.crew=9;apOrder.ap=0;
assert.throws(()=>game.startBattle(apOrder,{targetId:"south_dock",leaderIds:["player"],troops:9,tactic:"steady"}),/not enough crew/);
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，报 `开战消耗1个行动点` 处 `3 !== 2`

- [ ] **Step 3: 加检查与扣点**

`app.js:200`，在

```javascript
  if(s.crew<10)throw new Error("not enough crew");
```

之后插入：

```javascript
  if(s.ap<1)throw new Error("no action point");                                   // 必须排在 battleSession/crew 检查之后，既有测试依赖那两条的错误信息
```

`app.js:210` 附近，把 Task 2 加的那行

```javascript
  s.crew-=troops;                                                                 // 人立刻离开能战池，直到 finishBattle 才分流回整补/养伤
```

改成

```javascript
  s.ap--;s.crew-=troops;                                                          // 人立刻离开能战池，直到 finishBattle 才分流回整补/养伤
```

- [ ] **Step 4: 修正两处说明文字**

`app.js:448` 的 `renderHall`，把

```
行动点可以养人、攢钱、查情报；血拼本身不消耗行动点，但会消耗真实人手。
```

改成

```
行动点可以养人、攢钱、查情报。发起血拼额外消耗 1 个行动点，出战的人手要几个月才归队。
```

`app.js:479` 的 `renderBattle`，把

```javascript
<div class="section-head"><h2>血拼计划</h2><span>发起进攻不消耗行动点</span></div>
```

改成

```javascript
<div class="section-head"><h2>血拼计划</h2><span>发起进攻消耗 1 行动点</span></div>
```

- [ ] **Step 5: 让 `launchBattle` 挡住行动点不足**

`app.js:494`，在

```javascript
  if(S.crew<10){toast("至少需要10名人手才能开战");return false}
```

之后插入：

```javascript
  if(S.ap<1){toast("行动点已用完，这个月打不了了");return false}
```

- [ ] **Step 6: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: 无输出、退出码 0

- [ ] **Step 7: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "feat(battle): 发起血拼消耗 1 行动点"
```

---

### Task 7: 存档迁移与中断战斗的人手归还

**Files:**
- Modify: `app.js:427-433`（`normalizeState`）
- Test: `tests/structure.test.mjs`（文件末尾追加）

**背景：** `normalizeState` 在校验失败时会把 `s.battleSession` 置 `null`。旧模型下这没有代价；新模型下出战的人已经从 `s.crew` 扣掉，直接丢弃会让他们凭空蒸发。必须还回 `s.regroup`。

- [ ] **Step 1: 写失败的测试**

在 `tests/structure.test.mjs` 末尾追加：

```javascript
// ---- 存档迁移 ----
// 旧存档没有三池字段，载入后必须补齐而不是变成 NaN。
const legacy=game.createInitialState("沈旧档","yi","standard");
delete legacy.regroup;delete legacy.wounded;
const migrated=game.normalizeState(JSON.parse(JSON.stringify(legacy)));
assert.equal(migrated.regroup,0,"旧存档缺失的整补池补0");
assert.equal(migrated.wounded,0,"旧存档缺失的养伤池补0");
assert.equal(migrated.crew,42,"旧存档的 crew 原样视为能战");

// 脏数据不得变成 NaN
const dirty=game.createInitialState("沈脏档","yi","standard");
dirty.regroup="abc";dirty.wounded=-5;
const cleaned=game.normalizeState(JSON.parse(JSON.stringify(dirty)));
assert.equal(cleaned.regroup,0);
assert.equal(cleaned.wounded,0);

// 中断的战斗：出战的人已经离池，丢弃会话时必须还回整补池，否则凭空蒸发
const aborted=game.createInitialState("沈中断","yi","standard");
aborted.crew=200;
game.startBattle(aborted,{targetId:"south_dock",leaderIds:["player"],troops:60,tactic:"steady"});
assert.equal(aborted.crew,140);
aborted.battleSession.stage=99;                                    // 制造一个 validBattleSession 会拒绝的会话
const rescued=game.normalizeState(JSON.parse(JSON.stringify(aborted)));
assert.equal(rescued.battleSession,null,"损坏的会话必须丢弃");
assert.equal(rescued.regroup,60,"出战的60人要还进整补池");
assert.equal(game.totalCrew(rescued),200,"总人手不得因为存档损坏而减少");
```

- [ ] **Step 2: 跑测试确认失败**

Run: `node tests/structure.test.mjs`
Expected: FAIL，报 `旧存档缺失的整补池补0` 处 `undefined !== 0`

- [ ] **Step 3: 补齐迁移逻辑**

`app.js:431`，把

```javascript
  s.winStreak=Number.isFinite(s.winStreak)?Math.max(0,s.winStreak):0;
  if(!validBattleSession(s)){if(s.battleSession)log(s,"warn","上一场血拼中断，队伍已经撤回老街。");s.battleSession=null}
```

改成

```javascript
  s.winStreak=Number.isFinite(s.winStreak)?Math.max(0,s.winStreak):0;
  s.crew=Number.isFinite(s.crew)?Math.max(0,Math.round(s.crew)):0;
  s.regroup=Number.isFinite(s.regroup)?Math.max(0,Math.round(s.regroup)):0;
  s.wounded=Number.isFinite(s.wounded)?Math.max(0,Math.round(s.wounded)):0;
  // 出战的人在 startBattle 就离开了能战池。丢弃损坏的会话时不还人，他们就凭空蒸发了。
  if(!validBattleSession(s)){if(s.battleSession){s.regroup+=Math.max(0,Math.round(Number(s.battleSession.troops)||0));log(s,"warn","上一场血拼中断，队伍已经撤回老街整补。")}s.battleSession=null}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `node --check app.js && node tests/structure.test.mjs`
Expected: 无输出、退出码 0

- [ ] **Step 5: 提交**

```bash
git add app.js tests/structure.test.mjs
git commit -m "fix(save): 迁移三池字段，中断的战斗要把人还回整补池"
```

---

### Task 8: 平衡回归测试

**Files:**
- Create: `tests/balance.test.mjs`
- Delete: `tests/rush.sim.mjs`
- Modify: `README.md:32-35`（检查命令块）

**背景：** 这是防止"5 分钟通关"再次发生的护栏。Part 1 完成后只断言一条**下界**（莽夫策略不能再在个位数月份通关）；完整阈值表在 Part 3 数值校准后补齐。

- [ ] **Step 1: 新建测试文件**

创建 `tests/balance.test.mjs`：

```javascript
// 平衡回归测试：用固定种子的策略机器人跑完整局，锁住难度曲线。
// 这里断言的是"通关有多难"，不是"代码对不对"——数值一旦被改松，这个文件会先叫。
import assert from "node:assert/strict";
import {createRequire} from "node:module";

const require=createRequire(import.meta.url);
const game=require("../app.js");

function seeded(seed){return()=>{seed=(seed*1664525+1013904223)>>>0;return seed/4294967296}}

// 莽夫：行动点优先招人合练，剩下的全砸进强攻，只要评估到"优势"就打。
// 它代表玩家能采取的最激进的推进速度——通关月份的下界由它给出。
function runRush(seed,difficulty){
  const rng=seeded(seed),s=game.createInitialState("莽夫","wei",difficulty);
  for(let guard=0;guard<600&&!s.ended&&game.ownTerritories(s).length<8;guard++){
    // 留 1 点给开战，其余换成人手和训练
    while(s.ap>1&&(game.applyAction(s,"recruit_crew")||game.applyAction(s,"train")||game.applyAction(s,"business")));
    if(s.ap>=1&&s.crew>=10){
      const leaders=s.officers.filter(o=>o.side==="player"&&!o.injured)
        .sort((a,b)=>(b.stats.force+b.stats.command)-(a.stats.force+a.stats.command)).slice(0,3).map(o=>o.id);
      const troops=Math.max(10,Math.round(s.crew*.9));
      let best=null;
      for(const id of game.attackableTerritories(s)){
        const est=game.estimateBattle(s,id,leaders,troops,"assault");
        if(est.ratio>=1.28&&(!best||est.ratio>best.ratio))best={id,ratio:est.ratio};
      }
      if(best){
        game.startBattle(s,{targetId:best.id,leaderIds:leaders,troops,tactic:"assault"},rng);
        while(s.battleSession)game.applyStageChoice(s,"press",rng);
      }
    }
    if(game.ownTerritories(s).length>=8)break;
    if(!game.advanceMonth(s,true))break;
  }
  return{months:s.month,owned:game.ownTerritories(s).length,unified:game.ownTerritories(s).length===8,ended:s.ended,reason:s.endingReason,poolsOk:s.crew>=0&&s.regroup>=0&&s.wounded>=0};
}

const RUNS=20;
const rush=[];
for(let i=0;i<RUNS;i++)rush.push(runRush(20260731+i*7919,"standard"));
const unified=rush.filter(r=>r.unified);
const fastest=Math.min(...rush.map(r=>r.unified?r.months:Infinity));
const median=a=>a.length?a.slice().sort((x,y)=>x-y)[Math.floor(a.length/2)]:null;

console.log(`莽夫·标准：${unified.length}/${RUNS} 局一统，最快 ${Number.isFinite(fastest)?fastest:"—"} 月，中位数 ${median(unified.map(r=>r.months))??"—"} 月`);

// Part 1 的护栏，只断言本 Part 真正保证的东西：
// 一统需要 8 场仗（7 块地 + 中央港区），血拼每场 1 行动点、每月 3 点且只能打一场
// —— 因此最快只能在第 7 个月完成。这条断言精确锁死"血拼不消耗行动点"这个根因。
// 更严的阈值（中位数≥30、通关率<60%）要等 Part 2 的敌方数值与 AI 落地后，在 Part 3 补齐。
assert.ok(!Number.isFinite(fastest)||fastest>=7,`最快通关 ${fastest} 月，行动点闸门没有生效（8场仗×每月至多1场 ⇒ 至少 7 月）`);
assert.ok(rush.every(r=>r.months<=200),"机器人不得死循环");

// 人手守恒：三个池子都不得为负，也不得出现凭空生成的人。
for(const r of rush)assert.ok(r.poolsOk,`第 ${r.months} 月出现负数人手池`);
```

- [ ] **Step 2: 跑测试确认通过**

Run: `node tests/balance.test.mjs`
Expected: 打印一行统计（例如 `莽夫·标准：18/20 局一统，最快 8 月，中位数 11 月`），退出码 0

若最快通关仍 < 7 月，**不要放宽断言** —— 说明 Task 6 的行动点扣除没有真正生效，回去检查 `startBattle` 是否走到了 `s.ap--`。

- [ ] **Step 3: 删掉一次性诊断脚本**

```bash
rm tests/rush.sim.mjs
```

- [ ] **Step 4: 更新 README**

`README.md:32-35`，把

```bash
node --check app.js
node tests/structure.test.mjs
python3 build_single.py
```

改成

```bash
node --check app.js
node tests/structure.test.mjs
node tests/balance.test.mjs
python3 build_single.py
```

- [ ] **Step 5: 全量跑一遍**

Run: `node --check app.js && node tests/structure.test.mjs && node tests/balance.test.mjs`
Expected: 只有 balance 那一行统计输出，退出码 0

- [ ] **Step 6: 提交**

```bash
git add tests/balance.test.mjs README.md
git rm --cached tests/rush.sim.mjs 2>/dev/null || true
git commit -m "test(balance): 加入平衡回归测试，锁住通关月份下界"
```

---

## Part 1 完成标准

- [ ] `node --check app.js` 通过
- [ ] `node tests/structure.test.mjs` 通过
- [x] `node tests/balance.test.mjs` 通过，且最快通关 ≥ 6 月（对比改动前：第 0 月）

> **实施时修正：** 本计划原写"8 场仗 ⇒ 至少 7 月"，算错了。地图 8 块地、玩家开局已有老街，
> 一统只需 **7 场仗**（6 块敌方地盘 + 中央港区），因此下界是第 **6** 月。实测最快 6 月、中位数 6 月。
>
> **实测暴露的 Part 1 局限：** 平衡测试打印的"因兵力不足打不了"当前是 **0** —— 兵力状态机装好了，
> 但敌方驻防还是旧数值（24～82），机器人用半支队伍也能打出"优势"，闸门一次都没咬住。
> 让它真正咬住是 Part 2 的事（驻防上调约 50% + 敌方随地盘成长）。Part 3 校准时应把该数字断言成 > 0。
- [ ] 手动开一局确认：打完一仗后议事堂显示大量人手处于"整补/养伤"，且当月无法再次开战

**Part 1 之后游戏仍然偏简单** —— 敌方驻防还是旧数值、AI 还不会主动扩张。那是 Part 2 的事。此时不要为了让曲线好看而擅自调 `TERRITORY_DEFS` 的 `guard`。
