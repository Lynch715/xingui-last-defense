# 三段式血拼 Implementation Plan · Part 3（Task 9–12）

> 承接 `2026-07-29-三段式血拼-part2.md`。

---

## Task 9: 存档校验与旧档迁移

`battleSession` 是纯新增字段，**不改 `VERSION`**（升版会让所有旧存档失效）。缺失即 null，照 `insolvencyMonths` 的既有做法补默认值。

**Files:**
- Modify: `app.js`（`normalizeState`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写失败测试**

```js
const mig=game.createInitialState("沈迁移","yi","standard");
delete mig.battleSession;delete mig.winStreak;
assert.equal(game.normalizeState(mig).battleSession,null,"旧档缺字段应补 null");
assert.equal(mig.winStreak,0);

const bad=game.createInitialState("沈坏档","yi","standard");
bad.crew=120;
game.startBattle(bad,{targetId:"south_dock",leaderIds:["player"],troops:60,tactic:"steady"});
bad.battleSession.stage=9;
assert.equal(game.normalizeState(bad).battleSession,null,"stage 越界的会话必须丢弃");

const bad2=game.createInitialState("沈坏地","yi","standard");
bad2.crew=120;
game.startBattle(bad2,{targetId:"south_dock",leaderIds:["player"],troops:60,tactic:"steady"});
bad2.battleSession.targetId="不存在的地盘";
assert.equal(game.normalizeState(bad2).battleSession,null,"目标地盘不存在的会话必须丢弃");

const ok=game.createInitialState("沈好档","yi","standard");
ok.crew=120;
game.startBattle(ok,{targetId:"south_dock",leaderIds:["player"],troops:60,tactic:"steady"});
const roundTrip=game.normalizeState(JSON.parse(JSON.stringify(ok)));
assert.ok(roundTrip.battleSession,"完好会话经 JSON 往返后必须保留");
assert.deepEqual(game.stageOptions(roundTrip,roundTrip.battleSession),game.stageOptions(ok,ok.battleSession),"往返后重建的选项必须一致");
const cont=game.applyStageChoice(roundTrip,"hold",()=>.5);
assert.equal(cont.ended,false,"往返后必须能继续推进");
```

- [ ] **Step 2: 运行确认失败**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node tests/structure.test.mjs`
Expected: `AssertionError: stage 越界的会话必须丢弃`

- [ ] **Step 3: 实现**

在 `app.js` 中 `function normalizeState(` **之前**插入：

```js
function validBattleSession(s){
  const b=s.battleSession;
  if(!b||typeof b!=="object")return false;
  if(!TERRITORY_DEFS[b.targetId]||!s.territories[b.targetId])return false;
  if(!Number.isFinite(b.stage)||b.stage<1||b.stage>3)return false;
  if(!Number.isFinite(b.momentum)||!Number.isFinite(b.ratio)||!Number.isFinite(b.troops))return false;
  if(!b.mods||typeof b.mods!=="object"||!Array.isArray(b.log))return false;
  return Array.isArray(b.leaderIds)&&b.leaderIds.some(id=>{const o=officer(s,id);return o&&o.side==="player"});
}
```

在 `normalizeState` 内，`s.flags.debtCrisisQueued=false;` 之后、`return s` 之前插入：

```js
  s.winStreak=Number.isFinite(s.winStreak)?Math.max(0,s.winStreak):0;
  // 人手已按段扣过，丢弃损坏会话是安全的：最坏只是丢一块没打下来的地。
  if(!validBattleSession(s)){if(s.battleSession)log(s,"warn","上一场血拼中断，队伍已经撤回老街。");s.battleSession=null}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node --check app.js && node tests/structure.test.mjs`
Expected: `structure and core-loop tests passed`

- [ ] **Step 5: 提交**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大/app.js 模拟社团老大/tests/structure.test.mjs
git commit -m "feat(battle): validate and migrate battleSession in normalizeState

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10: 战斗视图（面板内，不走 modalQueue）

**Files:**
- Modify: `app.js`（`renderBattle` 开头分支；新增 `renderBattleSession`）
- Modify: `style.css`

- [ ] **Step 1: 加样式**

在 `style.css` 末尾追加：

```css
.momentum-wrap{margin:14px 0}
.momentum-rail{position:relative;height:12px;border-radius:7px;background:linear-gradient(90deg,rgba(201,75,75,.5),rgba(255,255,255,.08) 50%,rgba(95,191,130,.5));border:1px solid var(--line);overflow:hidden}
.momentum-rail i{position:absolute;top:0;bottom:0;width:3px;background:var(--gold);box-shadow:0 0 10px var(--gold)}
.momentum-label{display:flex;justify-content:space-between;font-size:11px;color:var(--muted);margin-top:6px}
.stage-scroll{display:grid;gap:10px;margin:14px 0}
.stage-done{border-left:2px solid var(--gold2);padding:9px 13px;background:#0a1011;border-radius:0 10px 10px 0}
.stage-done time{color:var(--gold);font-size:11px;font-weight:700;display:block;margin-bottom:4px}
.stage-done p{font-size:12px;color:var(--muted);line-height:1.6;margin:0}
.proposal-grid{display:grid;gap:9px;margin-top:12px}
.proposal-btn{text-align:left;border:1px solid var(--line);background:#11191a;border-radius:10px;padding:12px 14px;cursor:pointer;display:grid;gap:3px}
.proposal-btn:hover{border-color:var(--gold)}
.proposal-btn b{font-size:13px}
.proposal-btn small{font-size:11px;color:var(--muted)}
.proposal-btn cite{font-style:normal;font-size:10px;color:var(--gold)}
.proposal-btn.quit{border-style:dashed;opacity:.82}
```

- [ ] **Step 2: 加渲染函数**

在 `app.js` 的 `function renderBattle(` **之前**插入：

```js
function renderBattleSession(){
  const b=S.battleSession,panel=$("panel"),place=TERRITORY_DEFS[b.targetId].name;
  const pos=clamp((b.momentum+100)/2,0,100),opts=stageOptions(S,b);
  panel.innerHTML=`<section class="hero-panel"><span class="eyebrow">${esc(STAGE_NAMES[b.stage-1])} · ${b.stage}/3</span><h2>${esc(place)}，${esc(STAGE_NAMES[b.stage-1])}</h2>
    <div class="momentum-wrap"><div class="momentum-rail"><i style="left:calc(${pos}% - 1px)"></i></div>
    <div class="momentum-label"><span>对方占上风</span><b>${b.momentum>=0?"+":""}${b.momentum}</b><span>我方占上风</span></div></div>
    ${metrics([[S.crew,"剩余人手"],[b.losses,"本场折损"],[b.troops,"投入"],[tacticMeta(b.tactic).name,"战术"]])}</section>
    <div class="stage-scroll">${b.log.map(x=>`<article class="stage-done"><time>${esc(x.name)}</time><p>${esc(x.text)}</p></article>`).join("")}</div>
    <div class="section-head"><h2>接下来怎么办</h2><span>${esc(place)} · 第 ${b.stage} 段</span></div>
    <div class="proposal-grid">${opts.map(o=>`<button class="proposal-btn ${o.id==="withdraw"?"quit":""}" data-choice="${o.id}">${o.speaker?`<cite>${esc(o.speaker)}</cite>`:""}<b>${esc(o.text)}</b><small>${esc(o.effect)}</small></button>`).join("")}</div>`;
  panel.querySelectorAll("[data-choice]").forEach(btn=>btn.addEventListener("click",()=>{
    const res=applyStageChoice(S,btn.dataset.choice);
    renderAll();
    if(res.ended)announceBattleResult(res.report);
  }));
}
```

在 `function renderBattle(...){` 的 `const panel=$("panel"),targets=attackableTerritories(S);` **之前**插入分支：

```js
  if(S.battleSession)return renderBattleSession();
```

- [ ] **Step 3: 手工验证**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node --check app.js && node tests/structure.test.mjs && python3 build_single.py`
Expected: 语法通过、测试通过、构建成功。（`announceBattleResult` 在 Task 11 实现；本步只需语法通过，实际点击留到 Task 12 验证。）

- [ ] **Step 4: 提交**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大/app.js 模拟社团老大/style.css
git commit -m "feat(battle): in-panel battle view with momentum rail

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 11: 战果 modal 与结局时序

**这一步是本计划最容易埋回旧 bug 的地方。** 三段决策在面板里完成，但**最终战果必须仍入 `enqueue`**：否则终局之战会走成 `finishBattle → checkVictory → endGame →` 队列为空 `→ flushEnding()` 立刻揭晓，结局会盖住玩家还没看的最后一战战果——正是已修复的 P1 的翻版。

**Files:**
- Modify: `app.js`（替换 `launchBattle`；新增 `announceBattleResult`）
- Test: `tests/structure.test.mjs`

- [ ] **Step 1: 写测试**

```js
const seqWin=game.createInitialState("沈终局序","wei","standard");
for(const id of Object.keys(seqWin.territories))if(id!=="central_harbor")seqWin.territories[id].owner="player";
seqWin.crew=400;seqWin.morale=100;seqWin.territories.central_harbor.guard=6;
const finalRep=game.resolveBattle(seqWin,{targetId:"central_harbor",leaderIds:["player","zhaokui"],troops:300,tactic:"steady"},()=>.99);
assert.equal(finalRep.won,true);
assert.equal(seqWin.ended,true);
assert.equal(seqWin.endingReason,"unified");
assert.equal(seqWin.battleSession,null,"结局后不得残留会话");
assert.equal(finalRep.stages.length,3,"终局战报仍保留三段");
```

- [ ] **Step 2: 运行确认**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node tests/structure.test.mjs`
Expected: `structure and core-loop tests passed`

- [ ] **Step 3: 实现**

把整个 `function launchBattle(){...}` 替换为：

```js
// 战果仍走一次 enqueue：endGame 会清空旧队列并挂起 pendingEnding，
// 由 pumpModal 在队列排空后 flushEnding()。少了这次入队，结局会盖住最后一战的战果。
function announceBattleResult(report){
  if(!report)return;
  const tone=report.outcome==="win"?"血拼战报":report.outcome==="retreat"?"鸣金收兵":"血拼战报";
  const title=report.outcome==="win"?`${report.targetName}的招牌换了`:report.outcome==="retreat"?`队伍从${report.targetName}退了回来`:`${report.targetName}没能拿下`;
  enqueue({title,portrait:officer(S,report.leaders[0])?.portrait,
    body:`<div class="result-score"><span>和联胜</span><strong>${report.outcome==="win"?"胜":report.outcome==="retreat"?"撤":"败"}</strong><span>${esc(report.targetName)}</span></div>${report.stages.map(x=>`<div class="battle-stage"><time>${esc(x.name)}</time><div><p>${esc(x.text)}</p></div></div>`).join("")}${report.injured.length?`<p style="color:var(--red)">${esc(report.injured.join("、"))}在本场受伤。</p>`:""}`,
    options:[option(report.outcome==="win"?"把和联胜的招牌挂上去":"整队，这场不算完",`折损 ${report.losses} 人`,()=>{})]},tone);
}
function launchBattle(){
  if(!S){toast("当前存档已经失效");return false}
  if(S.crew<10){toast("至少需要10名人手才能开战");return false}
  if(!battleDraft.leaderIds.length){toast("至少选择一名头目");return false}
  try{
    const target=battleDraft.targetId;
    if(S.flags.cashDealUntil&&S.month<=S.flags.cashDealUntil&&target==="new_city"){ownedOfficers(S).filter(o=>o.id!=="player").forEach(o=>o.loyalty=clamp(o.loyalty-5));delete S.flags.cashDealUntil;log(S,"warn","你撕碎了方景曜那张支票上的承诺。")}
    startBattle(S,{...battleDraft});
    battleDraft.leaderIds=[];saveGame();renderAll();return true
  }catch(error){
    if(typeof console!=="undefined")console.error("[雾港] 开战失败",error);
    toast(error?.message==="target not attackable"?"目标地盘已经无法进攻，请重新选择":error?.message==="no leaders"?"没有可用头目参战":error?.message==="not enough crew"?"至少需要10名人手才能开战":"开战失败，当前进度没有变化");
    renderAll();return false
  }
}
```

- [ ] **Step 4: 运行确认通过**

Run: `cd "/Users/ruis/Claude/Projects/html游戏/模拟社团老大" && node --check app.js && node tests/structure.test.mjs && python3 build_single.py`
Expected: 全部通过

- [ ] **Step 5: 提交**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大/app.js 模拟社团老大/tests/structure.test.mjs
git commit -m "feat(battle): route final report through modal queue to preserve ending order

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 12: 浏览器端全流程验证

无头测试覆盖不到界面与时序。本任务在真实浏览器里跑一遍。

**Files:** 无（仅验证）

- [ ] **Step 1: 起服务**

用 `mcp__Claude_Preview__preview_start`，配置名 `fog-harbor`（`.claude/launch.json` 已存在）。

- [ ] **Step 2: 载入构建产物（带缓存破坏参数）**

浏览器会缓存 `app.js`，务必对 URL 加 `?cb=<随机>`，或直接载入单文件版：
`http://localhost:8777/<urlencoded 模拟社团老大-单文件版.html>?cb=1`

先断言 `typeof startBattle === "function"` 确认加载的是新代码，否则后续结论全部无效。

- [ ] **Step 3: 走完一场三段血拼**

新开局 → 序章 ×3 → 设 `S.crew=200; S.morale=90` → 地盘页点「制定进攻计划」→ 开战。
逐项确认：

- 面板显示势条与「开局 · 1/3」
- 第 1 段**没有**鸣金，第 2 段起出现
- 选项数 ≤ 5 且恒含「稳住阵型」
- 每段结束后战报卷轴累加一条，`S.crew` 下降
- 三段走完弹出战果 modal，`S.battleSession === null`

- [ ] **Step 4: 验证刷新续战（堵死读档大法）**

打到第 2 段时 `location.reload()`，点「继续」。确认：

- 回到**同一段**（`S.battleSession.stage` 不变）
- `S.crew` 与刷新前一致（人已按段扣过）
- `stageOptions` 重建结果与刷新前一致

- [ ] **Step 5: 验证撤退**

第 2 段选鸣金收兵。确认 `outcome==="retreat"`、不夺地、士气 −6、战果 modal 标题为「队伍从…退了回来」。

- [ ] **Step 6: 验证终局时序**

占七地 + 打中央港区并取胜。确认顺序为：**战果 modal → 关掉 → 结局**，结局出现时 `modalMask` 已隐藏、无弹窗覆盖。

- [ ] **Step 7: 控制台零警告**

`mcp__Claude_Preview__preview_console_logs` 取 `level:"warn"`，Expected: `No console logs.`

- [ ] **Step 8: 提交验证结论**

```bash
cd "/Users/ruis/Claude/Projects/html游戏"
git add 模拟社团老大
git commit -m "chore: rebuild single-file bundle after battle rework

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## 自查：设计文档覆盖情况

| 设计文档章节 | 落实任务 |
|---|---|
| 3.1 可续存会话 | Task 1、9 |
| 3.2 不走 modalQueue | Task 10（面板）、11（仅战果入队） |
| 3.3 选项为推导结果 | Task 2（幂等测试）、9（往返一致测试） |
| 4 势的数学 / ±29.5% | Task 3、4（平衡回归） |
| 4.4 逐段伤亡 | Task 3 |
| 4.5 三态结局与撤退代价 | Task 4、5 |
| 5.1 组装顺序 | Task 2（已改为上限 5，见 Part 1 偏离说明） |
| 5.2 提议表 | Task 6 |
| 5.3 被动特性 | Task 1（沈川/谢九/叶蓉）、4（唐霁）、8（测试） |
| 5.4 单挑 | Task 7 |
| 6.1–6.3 存档与迁移 | Task 9 |
| 6.4 界面分支 | Task 10 |
| 6.5 战果时序 | Task 11 |
| 7 resolveBattle 包装器 | Task 4 |
| 8 九项测试 | 1/2/3(#2) · 4(#1) · 5(#3) · 6(#7) · 7(#6) · 9(#4#5#8) · 11(#9) |
